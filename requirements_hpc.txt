# LexCausa HPC backend requirements

# ── HuggingFace ecosystem ─────────────────────────────────────────────────────
transformers>=4.40.0,<4.50.0
huggingface-hub>=0.20.0
sentence-transformers>=2.2.0,<3.0.0

# ── LangChain ───────
langchain>=0.2.0,<1.0.0
langchain-core>=0.2.0,<1.0.0

# ── Cerberus (llama.cpp) inference backend (LLM_BACKEND=local) ─────────────────
# The `cerberus` package is installed separately, editable, from its checkout:
#     pip install -e /path/to/Cerberus
# It provides `from cerberus import CerberusClient` and uses the OpenAI SDK to
# reach the served llama.cpp endpoints (endpoints.json written by `cerberus up`).
openai>=2.44.0,<3.0.0

# ── Neo4j knowledge base ──────────────────────────────────────────────────────
neo4j>=5.0.0,<6.0.0

# ── Web framework (required import by api_server.py — no server is started) ──
flask>=3.0.0,<4.0.0
flask-cors>=4.0.0,<7.0.0

# ── Data / stats ──────────────────────────────────────────────────────────────
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
scipy>=1.12.0
scikit-learn>=1.4.0,<2.0.0
pyarrow>=15.0.0

# ── Config / utilities ────────────────────────────────────────────────────────
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
sqlalchemy>=2.0.30,<3.0.0
textstat>=0.7.0,<1.0.0
datasets>=2.0.0,<3.0.0
